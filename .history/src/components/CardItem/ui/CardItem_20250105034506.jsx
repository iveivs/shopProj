import { Button } from "@/ui/Button";
import cls from "./CardItem.module.scss"

const CardItem = (props) => {
    const { id, img, key, title, product, ingredients } = props;
    return 
};

export { CardItem };
